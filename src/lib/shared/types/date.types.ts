/** Fecha civil en formato YYYY-MM-DD. */
export type IsoDate = `${number}-${number}-${number}`;

/** Fecha y hora ISO 8601 en la que ocurrió un evento dentro de la aplicación. */
export type IsoDateTime = string;
